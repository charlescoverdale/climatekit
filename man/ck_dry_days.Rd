% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precipitation.R
\name{ck_dry_days}
\alias{ck_dry_days}
\title{Maximum Consecutive Dry Days}
\usage{
ck_dry_days(precip, dates, threshold = 1, period = "annual")
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm).}

\item{dates}{Date vector of the same length as \code{precip}.}

\item{threshold}{Numeric. Dry day threshold in mm (default 1).}

\item{period}{Character. Aggregation period: \code{"annual"} (default) or
\code{"monthly"}.}
}
\value{
A data frame with columns \code{period}, \code{value}, \code{index}, and \code{unit}.
}
\description{
Maximum number of consecutive days with precipitation below a threshold.
This is the ETCCDI \strong{CDD} index.
}
\details{
Note: \code{CDD} also denotes climpact's cooling degree days, which is
unrelated and is implemented in \code{\link[=ck_cooling_degree_days]{ck_cooling_degree_days()}}. In
\code{\link[=ck_compute]{ck_compute()}} the name \code{"cdd"} resolves here, to consecutive dry days;
\code{"consecutive_dry_days"} is accepted as an unambiguous alias.
}
\examples{
dates <- as.Date("2024-01-01") + 0:9
precip <- c(0, 0, 5, 0, 0, 0, 2, 0, 0, 0)
ck_dry_days(precip, dates)
}
