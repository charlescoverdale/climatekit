% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatwave.R
\name{ck_hwa}
\alias{ck_hwa}
\title{Heatwave Amplitude (HWA)}
\usage{
ck_hwa(
  tmax,
  dates,
  ref_start = 1961L,
  ref_end = 1990L,
  min_spell = 3L,
  mode = c("excess", "absolute")
)
}
\arguments{
\item{tmax}{Numeric vector of daily maximum temperatures (degrees C).}

\item{dates}{Date vector of the same length as \code{tmax}. Must contain
data covering the reference period.}

\item{ref_start, ref_end}{Integer. Reference period boundary years
(inclusive). Defaults to 1961 and 1990.}

\item{min_spell}{Integer. Minimum spell length in days (default 3,
the ET-SCI standard).}

\item{mode}{One of \code{"excess"} (default) or \code{"absolute"}. See details.}
}
\value{
A data frame with columns \code{period}, \code{value}, \code{index}, and \code{unit}.
}
\description{
ET-SCI heatwave family index. Reports the peak magnitude of daily
Tmax across all heatwave days in the year. \code{mode = "excess"}
(default) gives the maximum of (Tmax - threshold). \code{mode = "absolute"} gives the maximum raw Tmax across heatwave days
(matching Perkins-Alexander 2013). Returns \code{NA} for years with
no heatwaves.
}
\examples{
set.seed(1)
dates <- seq(as.Date("1961-01-01"), as.Date("1991-12-31"), by = "day")
tmax <- 15 + 10 * sin(2 * pi * as.integer(format(dates, "\%j")) / 365) +
        rnorm(length(dates))
tail(ck_hwa(tmax, dates))
}
