% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precipitation.R
\name{ck_wet_days}
\alias{ck_wet_days}
\title{Maximum Consecutive Wet Days}
\usage{
ck_wet_days(precip, dates, threshold = 1, period = "annual")
}
\arguments{
\item{precip}{Numeric vector of daily precipitation (mm).}

\item{dates}{Date vector of the same length as \code{precip}.}

\item{threshold}{Numeric. Wet day threshold in mm (default 1).}

\item{period}{Character. Aggregation period: \code{"annual"} (default) or
\code{"monthly"}.}
}
\value{
A data frame with columns \code{period}, \code{value}, \code{index}, and \code{unit}.
}
\description{
Maximum number of consecutive days with precipitation at or above a
threshold. This is the ETCCDI \strong{CWD} index.
}
\details{
Note: the same letters CWD also denote the ET-SCI \strong{cold-wave duration}
index, which is unrelated and is implemented in \code{\link[=ck_cwd]{ck_cwd()}}. In
\code{\link[=ck_compute]{ck_compute()}} the name \code{"cwd"} resolves to cold-wave duration, not to
this function; use \code{"wet_days"} or the unambiguous alias
\code{"consecutive_wet_days"} for consecutive wet days.
}
\examples{
dates <- as.Date("2024-01-01") + 0:9
precip <- c(5, 3, 0, 2, 8, 1, 0, 0, 4, 6)
ck_wet_days(precip, dates)
}
